spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${DB_NAME:skydb}
    username: ${DB_USER:skyuser}
    password: ${DB_PASSWORD}
  jpa:
    properties:
      hibernate:
        show_sql: true

logging:
  level:
    com.challange.sky.api: DEBUG
    org.hibernate.SQL: DEBUG
