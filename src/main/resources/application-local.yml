spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/skydb
    username: skyuser
    password: skypass
  jpa:
    properties:
      hibernate:
        show_sql: true

logging:
  level:
    com.challange.sky.api: DEBUG
    org.hibernate.SQL: DEBUG
